#include <stdio.h>
#include <stdlib.h>

#include <stdio.h>

int main(int argc, char *argv[]) {
    if (argc >= 1) {
        /* Print the program name */
        printf("%s\n", argv[0]);
    }

    return 0;
}
